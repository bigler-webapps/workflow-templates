name: 'Register Uptime Kuma Monitors'
description: 'Reads a per-app YAML config and idempotently registers or updates monitors in Uptime Kuma. Notifications marked default:true (via sync-kuma-notifications) are auto-applied. Connects via SSH tunnel to the host (Kuma must be bound to 127.0.0.1:3001 there).'

inputs:
  config_path:
    description: 'Path to monitor YAML (relative to repo root).'
    required: false
    default: 'monitoring/monitor.yml'
  ssh_host:
    description: 'SSH host for tunnel into the Kuma server.'
    required: true
  ssh_user:
    description: 'SSH user (typically deploy).'
    required: true
  ssh_private_key:
    description: 'SSH private key (PEM contents).'
    required: true
  kuma_user:
    description: 'Automation user in Kuma.'
    required: true
  kuma_password:
    description: 'Password for the automation user.'
    required: true
  kuma_local_port:
    description: 'Server-side localhost port Kuma listens on (matches docker-compose port binding).'
    required: false
    default: '3001'
  prune:
    description: 'Set "true" to delete Kuma monitors not present in this config file. WARNING: deletes monitors from OTHER apps too if their names are absent here — only enable in single-source-of-truth setups.'
    required: false
    default: 'false'

runs:
  using: composite
  steps:
    - name: Setup Python
      uses: actions/setup-python@b64ffcaf5b410884ad320a9cfac8866006a109aa  # v5.6.0
      with:
        python-version: '3.11'

    - name: Install kuma deps
      shell: bash
      # uptime-kuma-api 1.3.0+ supports Kuma 2.x (Socket.IO v4 protocol).
      # 1.2.1 was tested against Kuma 1.x and fails with "Connection reset by
      # peer" during Socket.IO handshake against Kuma 2's server.
      run: pip install --quiet "uptime-kuma-api>=1.3.0,<2.0" "PyYAML==6.0.2"

    - name: Configure SSH key
      shell: bash
      env:
        SSH_PRIVATE_KEY: ${{ inputs.ssh_private_key }}
        SSH_HOST: ${{ inputs.ssh_host }}
      run: |
        set -euo pipefail
        mkdir -p ~/.ssh
        chmod 700 ~/.ssh
        printf '%s\n' "$SSH_PRIVATE_KEY" | tr -d '\r' > ~/.ssh/id_sync
        chmod 600 ~/.ssh/id_sync
        ssh-keyscan -H "$SSH_HOST" >> ~/.ssh/known_hosts 2>/dev/null

    - name: Open SSH tunnel to Kuma
      shell: bash
      env:
        SSH_HOST: ${{ inputs.ssh_host }}
        SSH_USER: ${{ inputs.ssh_user }}
        KUMA_PORT: ${{ inputs.kuma_local_port }}
      run: |
        set -euo pipefail
        ssh -i ~/.ssh/id_sync -o IdentitiesOnly=yes -o ExitOnForwardFailure=yes \
            -fNL "${KUMA_PORT}:localhost:${KUMA_PORT}" \
            "${SSH_USER}@${SSH_HOST}"
        for _ in $(seq 1 10); do
          if curl -sf "http://localhost:${KUMA_PORT}/" -o /dev/null; then
            echo "✅ Kuma reachable via tunnel"
            break
          fi
          sleep 1
        done

    - name: Sync monitors
      shell: bash
      env:
        KUMA_URL: http://localhost:${{ inputs.kuma_local_port }}
        KUMA_AUTOMATION_USER: ${{ inputs.kuma_user }}
        KUMA_AUTOMATION_PASSWORD: ${{ inputs.kuma_password }}
      run: |
        set -euo pipefail
        PRUNE_FLAG=""
        if [ "${{ inputs.prune }}" = "true" ]; then
          PRUNE_FLAG="--prune"
        fi
        python "${GITHUB_ACTION_PATH}/../../../scripts/kuma_sync.py" \
          monitors \
          --config "${{ inputs.config_path }}" \
          ${PRUNE_FLAG}

    - name: Close SSH tunnel
      if: always()
      shell: bash
      run: |
        pkill -f 'ssh -i .*/id_sync .* -fNL' || true
