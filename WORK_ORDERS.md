# WORK_ORDERS.md — workflow-templates

Work-order register for this repo. Lightweight directory (not the full orders):
one row per WO with its implementation status. Convention, schema, and maintenance
rules are defined centrally in `webapps/AGENTS.md` → "Work-Order Register".

**Scope note:** this register starts with the 2026-07 tailnet-resilience workstream;
older work is in `git log`.

## Workstream prefixes

| Prefix | Workstream |
|---|---|
| `TS-*` | Tailnet/Tailscale CI resilience (probe-guard, joins, CDN dependency) |
| `CI-*` | CI workflows / composite actions (non-tailnet) |
| `INF-*` | Shared with webapp-management — the `biglerconsult.infra` collection half of an INF work order. Same ID in both registers (one WO, two repos); the full order lives in `webapp-management/work-orders/`. |

Introduce a new prefix when none fits and add it here. New WOs always get a
prefixed ID; never reuse a bare flat number across workstreams.

## Register

| ID | Titel | Beschreibung | Datum | Status | Commit(s) | Notiz |
|---|---|---|---|---|---|---|
| CI-7 | Cap glibc malloc arenas in the backend pytest container (`MALLOC_ARENA_MAX=2`) | hram's PR #125 (`ci/backend`) kept OOM-killing (exit 137) on the shared self-hosted netcup runner (7.8GB RAM, 3 job slots shared org-wide, no `--memory` limit on the test container). Live-reproduced on the runner itself (ssh): a single memory-heavy geo-build test, run in complete isolation, peaked at ~7GB RSS and triggered a real kernel OOM kill (dmesg-confirmed) — vs ~1.9GB on a dev machine with more cores/RAM, consistent with glibc's per-thread malloc-arena allocation inflating RSS under genuine memory pressure. Added `MALLOC_ARENA_MAX=2` to the shared `app-ci.yml` backend pytest `docker run` — backward-compatible, correctness-neutral, benefits every repo on this same constrained runner. Full detail in hram's `WORK_ORDERS.md` FIX-7 (the primary WO; this row is the cross-repo landing). | 2026-08-07 | done | a741f2d | Tier 1 (mechanical env-var addition to a shared reusable workflow; no behavior/correctness change, only memory-allocation pattern). Live-tested on the reproduced OOM case before landing: helped (cleaner release between pipeline stages) but was NOT alone sufficient margin (still peaked near the ceiling) — hram's own fix (excluding the specific memory-heavy test from default CI via `@pytest.mark.slow`) is the primary mitigation; this is defense-in-depth for every other consumer of this template. Self-check, no separate reviewer per the Tier-1 gate. |
| CI-8 | kuma_sync must clear undeclared notification IDs, not only add declared ones | `_resolve_notification_ids` only ever ADDED resolved notifications to a monitor's `notification_ids`; it never set the key to an empty list, so `_build_monitor_kwargs` omitted `notificationIDList` entirely and `_monitor_changed` never compared it — a monitor's EXISTING Kuma-side assignment survived untouched whenever the declared config resolved to nothing for it. Discovered live while implementing `webapp-management` INF-9: turning `alert-email` off `default: true` would not actually detach it from the ~65 monitors that only ever had it via that default. Full order [`work-orders/CI-8.md`](work-orders/CI-8.md) | 2026-08-07 | done | 3e06b37 | Tier 2. Discovered mid-implementation of INF-9 (traced `_resolve_notification_ids` → `_build_monitor_kwargs` → `_monitor_changed` end-to-end against the post-INF-9 config), surfaced to the operator via AskUserQuestion before proceeding (three options offered: land INF-9's config-only and flag manual cleanup, extend kuma_sync.py to clear explicitly, or hold INF-9 entirely) — operator chose the code fix. Implemented directly (no Codex round — same-session continuation of CI-6, and this is a small, well-understood extension of code just reviewed clean); independent `reviewer` mandatory per the author≠reviewer rule regardless. New `had_explicit_names` tracking + `protect_unresolvable` guard replaces the old `if combined:` check: `notification_ids` is now SET (including empty `[]`) whenever nothing at all is declared for a monitor (the new INF-9-driven "deliberate none" case) — the ONE case that still does NOT set the key is explicit `notification_name` given, none resolved, AND no defaults either (CI-5's original protection, narrowed to the case it actually guards rather than applied to "nothing declared"). **This is a deliberate behaviour change to an established CI-5 guarantee** — the old test encoding "nothing declared → don't touch Kuma" was rewritten, not left passing by accident, to assert the new "nothing declared → clear explicitly" semantics; a new test pins the still-protected case. **Independent `reviewer` (first pass): one P1, fixed** — `sync_monitors_multi`'s existing graceful-degrade path (a `get_notifications()` failure continues the run with an empty notifications map, printing a warning) would, under the naive fix, have wiped every default-only monitor's real notification assignment on a mere transient API hiccup — the fetch failing is not the same as the config declaring nothing, and this is exactly the "silent, wrong" failure mode CI-8 exists to eliminate. Fixed with a `notifications_resolved` flag threaded from that degrade branch into `_resolve_notification_ids`, which now never sets the key at all while it's False, regardless of whether the monitor has an explicit name. Non-tautological integration test added (`test_multi_get_notifications_failure_does_not_wipe_existing_default_only_monitor`, fake API whose `edit_monitor`/`add_monitor` raise `AssertionError` if called) plus two unit tests on the flag. **Second independent `reviewer` pass on the fix: no findings** — confirmed the chain holds in the actual code (degrade branch → `notifications_resolved=False` → threaded to the call site → `protect_unresolvable` short-circuits True regardless of explicit names → key never set → nothing for `_monitor_changed` to diff), confirmed the new integration test is non-tautological (traced that it would genuinely fail pre-fix), confirmed no third call site needs the same protection, confirmed the `or` correctly adds the new case without weakening the original four CI-5 combinations. **Tests: 61 green** (was 58 after the first pass, 3 more for the degrade-path fix). **NOT live-verified** — acceptance is the next scheduled `kuma-sync` run after INF-9's default is turned off actually clearing `alert-email` from a previously-default-only monitor, operator-gated same as CI-4/5/6. |
| CI-6 | kuma_sync input handling: multiple notifications per monitor, and typed config values | Two defects that surfaced on 2026-08-07 while wiring the estate's second alert channel. (1) `_resolve_notification_ids` accepts exactly ONE `notification_name` per monitor plus the defaults, so a channel that must reach SOME monitors but not all cannot be attached where a relay already sits — accept a list, keep the singular form working. (2) Every value routed through `${...}` arrives as a string (`_expand_env` uses `os.path.expandvars`, and the composite action feeds it from GitHub Actions inputs, which are always strings), so Kuma's typed validation dies with `TypeError: '<' not supported between instances of 'str' and 'int'` — coerce known numeric/boolean fields after expansion. Full order [`work-orders/CI-6.md`](work-orders/CI-6.md) | 2026-08-07 | done | 0ff3110 | Tier 2. Codex was out of workspace credits (no output beyond echoing the WO), so the Codex→Claude fallback applied and this session implemented directly (independence flip: an independent `reviewer` was mandatory and was run — **no findings**). Item 1: `notification_name` now accepts a list as well as a string; every entry resolves independently and unions with the declared defaults exactly as before; the singular form is byte-identical (wraps to a 1-element list internally); one unresolvable name in a list warns and is skipped without aborting the rest; the CI-5 "never empty on its own" property holds for both forms (`notification_ids` is only set when the union is non-empty). Item 2: new `_coerce_typed_config` (driven by an explicit `_TYPED_CONFIG_FIELDS` dict — `smtpPort: int`, `smtpSecure: bool`) coerces only known fields in a notification's `config` dict, applied after `_expand_env` in `sync_notifications`; unlisted fields (including numeric-looking strings) pass through untouched, matching Risk 2. **Tests: 57 green** (was 47, 10 new) — mixed-list warn-and-skip, list+no-defaults non-empty, list-all-unresolvable-no-defaults sets nothing, string form unchanged, smtpPort/smtpSecure coercion, unlisted field untouched, already-typed values untouched, and an end-to-end `sync_notifications` test proving an env-expanded `smtpPort` arrives as `int` at the API boundary. **NOT live-verified** — the WO's own acceptance criterion (a two-notification dispatch confirmed in Kuma) is live-Kuma and operator-gated, same posture as CI-4/CI-5. Unblocks `webapp-management` INF-9. |
| CI-5 | Monitors created by the sync have no notification channel at all | Kuma does NOT apply a `default: true` notification to monitors created through the socket API — the client always sends `notificationIDList`, as `{}` when nothing is passed, so every monitor the sync creates is created with an explicitly empty assignment. Fix: attach the declared default notifications on creation (Kuma will not), resolve `notification_name` on the `multi` path as the single path already does, let `monitoring.extra` entries declare a relay, repair the already-created monitors rather than only future ones, and reconcile the undeclared `claude-relay` notification. Full order [`work-orders/CI-5.md`](work-orders/CI-5.md) | 2026-08-06 | done | e7f4956 | Tier 2. Codex was out of workspace credits (no assistant output beyond echoing the WO), so the Codex→Claude fallback applied and this session implemented directly (independence flip: an independent `reviewer` was mandatory and was run). Added `_load_default_notification_names` (resolves `isDefault` from the declared `notifications.yml` FILE, not Kuma's live state — Risk 4) and `_resolve_notification_ids` (unions declared defaults with a resolved `notification_name`, `pop()`s the key), wired into both `sync_monitors` and `sync_monitors_multi` — the latter never resolved `notification_name` at all before (the asymmetry the WO's scope item 2 names) and is the path CI actually uses (`webapp-management/.github/workflows/kuma-sync.yml`'s `multi` invocation). `monitoring.extra` entries declaring `notification_name` (scope 3) and repair of already-created monitors (scope 4) needed no separate code — both fall out of the same generic resolution feeding the existing CI-4 id-set comparison. New `--notifications-config` CLI flag (default `monitoring/notifications.yml`) resolves correctly against the production caller's cwd with **zero webapp-management changes** (verified: `kuma-sync.yml` checks out webapp-management unqualified before invoking the script). **Independent `reviewer`: no P1/P2**, one P3 (docstring overstated the "never empty" guarantee for the edge case where `notifications.yml` is itself missing/misconfigured AND the relay doesn't resolve — fixed, wording narrowed; behaviourally unchanged, low real-world likelihood since `discord-alerts` is always declared `default: true`). Reviewer specifically traced the new pre-loop `get_notifications()` call in `sync_monitors_multi` against the CI-4 `BudgetExceeded`/`WedgedApi`/recovery-branch test contracts and confirmed no regression (also independently re-derived by the orchestrator while writing the fix: the call is wrapped so `BudgetExceeded` propagates but any other failure degrades to "no default/relay resolution this pass" rather than crashing, so the existing per-app failure reporting still triggers from each app's own `get_monitors()` call). **Tests: 47 green** (was 37 after CI-4), 10 new. **NOT live-verified** — the WO's actual acceptance criterion ("force a breach and confirm a notification actually arrives") is live-Kuma and operator-gated, same posture as CI-4. **Scope item 5 (undeclared `claude-relay` notification) intentionally NOT touched by this diff** — the Implementation map scoped this WO to `scripts/kuma_sync.py` only; `notifications.yml` still declares no bare `claude-relay`, so the stray default-marked entry Kuma showed on `main-prod-health-disk` remains in Kuma until removed by hand or via a `sync-kuma-notifications` dispatch with `prune: true` (that dispatch needs explicit operator confirmation per AGENTS.md workflow-dispatch gate — flagged to the operator, not run here). | Tier 2. **Proven in the Kuma UI 2026-08-06** on `main-prod-health-disk`, hours after INF-6 created it: every notification toggle off, **including `discord-alerts`, which carries the Standard/isDefault badge**. `notifications.yml`'s own comment (*"auto-applied to every new monitor"*) describes the UI's behaviour, not the API's. **Blast radius:** every monitor created by the `multi` path since the move to the central `kuma-sync.yml` — not only the twelve health monitors. Monitors that predate the move, or that were edited rather than created, keep what they had (`edit_monitor` reads-then-merges, omitted fields survive); the gap is specific to creation. **Two defects behind it:** defaults are never attached, and `notification_name` is resolved only at `kuma_sync.py:560-565` (single path) while the `multi` path drops it — and `prod_relay` is merged only into auto-derived frontend/healthz specs (`:476-477`), never into `monitoring.extra`. Because the field is never sent, `_monitor_changed` never compares it, so a relay removed by hand in the UI is invisible to the sync. **Same defect as the notification-drift chip; one fix closes both, plus INF-6 scope item 4, whose stated fallback is currently unavailable.** **Priority:** without this, INF-2/INF-3/INF-6 are decorative — the self-reach gap narrows alert coverage to one host, this narrows it to none. Acceptance is not a toggle being on: force a breach and confirm an alert reaches a human, which has never once been demonstrated in this estate. |
| CI-4 | kuma-sync rewrites every monitor on every run, and retries on a dead socket | `scripts/kuma_sync.py` reports EVERY monitor as changed on EVERY run, so each sync performs a full write pass of all ~57 monitors against Kuma's single-threaded Socket.IO server. `_retry_call` then retries five times (~50 s) on the SAME api object without reconnecting, so once the socket wedges every remaining attempt is a guaranteed failure. Fix: prove which key permanently differs (log it), normalise that comparison, reconnect between retries like `_login` already does, add an overall run budget, and add the tests this script currently has none of. Full order [`work-orders/CI-4.md`](work-orders/CI-4.md) | 2026-08-06 | done | 4800964 | Tier 2. Evidence from two consecutive runs 2026-08-06: failed run 31081103662 (07:29) = 39 updated / **0 unchanged** / 18 retry storms, 24 min, gave up on 4 apps (cockpit, hpc-bridge, innoservice, kerzenziehen) and correctly skipped the prune; passing run 31083824555 (08:10) = 57 updated / **0 unchanged** / 2 retry storms. Never a single `unchanged` in either. Prime suspect for the permanent diff: `notificationIDList` is sent as a list (`spec["notification_ids"] = [notif_id]`) while Kuma echoes that field as an object keyed by id, so `_monitor_changed` short-circuits on `not isinstance(current, list)`; second candidate `accepted_statuscodes`. Reasoned from the code, NOT proven — hence scope item 0 logs the differing key first. Retry signature confirms defect 2: attempt 1 `TimeoutError`, attempts 2-4 `UptimeKumaException`, repeated across 18 monitors, while `_login` (which DOES `api.disconnect()` between attempts) recovers fine. Failure rate 4/15 runs, denser since the monitor count grew to 57 (INF-2 +18, INF-3 -6). **Blocking:** INF-3's remaining operator-gated `kuma-sync` dispatch to remove the six `*-health-swap` monitors IS the run that failed at 07:29 — prune skipped, monitors still present, so INF-3 and webapp-management INF-2 cannot reach `done`. Interim option (not a fix): the workflow's `app_filter` input limits a run to `webapp-management` only. Acceptance criterion: two consecutive runs where the second reports `updated: 0` and all monitors `unchanged`. **Implemented 2026-08-06.** Codex was out of workspace credits, so the Codex->Claude fallback applied and this session implemented directly (independence flip: an independent `reviewer` was mandatory and was run). **The Envelope's prime suspect was WRONG, and scope item 0 is what caught it.** Two independent reasons `notificationIDList` cannot have been the permanent diff on the failing runs: (a) CI runs the `multi` subcommand, and `sync_monitors_multi` never resolves `notification_name` -> `notification_ids`, so the key is never sent and `_monitor_changed` (which iterates `kwargs` only) can never compare it; (b) the client library already converts that field dict->list[int] on read. **The real root cause is `type`:** `uptime_kuma_api` converts a monitor's `type` into a `MonitorType` enum ON READ while the script sends the plain string `http`, and the old comparison used `str(current)` — for a `(str, Enum)` member that is `MonitorType.HTTP`, not `http`. Every monitor, every run, forever, which is exactly the observed signature (0 `unchanged` in either evidence run). The old comment at that line claiming `str()` normalises enums was itself the bug. Confirmed against the client library source (`parse_monitor_type`), not guessed. Fix: `_unwrap()` compares Enum members by `.value`. `notificationIDList` is normalised to an id SET anyway (list/dict/dict-of-bool all equal) per scope 1, with `false` meaning NOT assigned so real un-assignment drift is still corrected (Risk 1). Scope 0's logging is retained permanently. **Scope 2:** the Envelope's `mirror _login's disconnect` is insufficient on its own — `_retry_call` received an already-BOUND method of the dead api object, so reconnecting inside it would still call the dead connection, and a local rebind could not reach the caller. Introduced a `Session` holder; retries resolve the method BY NAME against the live connection. All 11 call sites rewired; no third retry layer added (the existing `recovery_attempt` loop is unchanged and still re-fetches `existing`). **Scope 3:** `Budget`/`BudgetExceeded`, default 600 s via `KUMA_SYNC_BUDGET_SECONDS`, checked before each monitor and before each retry backoff; abort skips the prune (same safety as the failed_apps guard, which is untouched). **Independent `reviewer`: one P1 + one P2 + three P3, all fixed.** P1 (R1) was real and would have defeated scope 3 entirely: `BudgetExceeded` was swallowed by two generic `except Exception` handlers (the per-app `get_monitors` fetch and the recovery re-fetch), which rewrote it as a per-app failure and CONTINUED to the next app — so a wedged Kuma would still not fail fast, and would exit with a misleading `N app(s) failed` instead of the budget message. Fixed with explicit `except BudgetExceeded: raise` guards at both sites. P2 (R2): no integration-level test drove `sync_monitors_multi` through budget exhaustion — which is precisely why R1 survived unit testing; three such tests added, and the key one was verified NON-TAUTOLOGICAL by temporarily removing the guard and confirming it reproduces the exact misreport. P3s: `main()` disconnected the stale api rather than the session's live one after a reconnect (fixed); the dead `bool` branch logged un-normalised values (branch deleted as unreachable — `_build_monitor_kwargs` whitelists keys and none are boolean); `BudgetExceeded` surfaced as a raw traceback on the single-app path (handler added in `main()`). **Tests: 30 green** in `scripts/tests/` — the script previously had NONE. `conftest.py` stubs `uptime_kuma_api` in `sys.modules` because the real package is not installed locally and the module `sys.exit(1)`s on ImportError; production import path untouched. **NOT live-verified.** The acceptance criterion is inherently a live one and is operator-gated: two consecutive real `kuma-sync` runs where the SECOND reports `updated: 0` and every monitor `unchanged`. Anything less means the permanent diff is still there. The reviewer additionally recommends a wedged-Kuma run to confirm the budget aborts as fast as intended. Note the ordering value: this unblocks INF-3's remaining prune dispatch, and `app_filter=webapp-management` remains the narrow-blast-radius option for that. **Separate defect found while mapping, NOT fixed here (out of scope, needs its own WO + operator decision):** because `sync_monitors_multi` never resolves `notification_name`, monitors synced by the CI path appear to get NO relay notification assigned at all — a monitor that goes down would alert nobody. Potentially more serious than the defect CI-4 fixes. **Root cause PROVEN against the installed fork, not inferred** (2026-08-06): `uptime-kuma-api-v2` **1.0.3** introspected directly in a scratch venv. `UptimeKumaApi.get_monitors` ends with `_convert_monitor_return(monitor)` / `int_to_bool(r, ["active"])` / **`parse_monitor_type(r)`** / `parse_auth_method(r)`, so `type` IS converted to a `MonitorType` enum on every read. `MonitorType` is a `(str, Enum)`: `str(MonitorType.HTTP)` == `MonitorType.HTTP` while `.value` == `http`, so the old comparison evaluated False (= differs) for EVERY monitor and the new one evaluates True. The same read path confirms `_convert_monitor_return` normalises `notificationIDList` dict->list[int], independently confirming the Envelope's suspect was wrong. **Second-cause candidate ruled out:** `accepted_statuscodes` is declared only by hpc-bridge (4 monitors) and webapp-management (health monitors), always as `["200"]`, which round-trips unchanged (the library only substitutes a default when the value is falsy). So `type` is the single cause, and it hits 100% of monitors because every monitor has one. **The fix is LIVE IMMEDIATELY — no tag or pin bump needed:** `webapp-management/.github/workflows/kuma-sync.yml` checks out this repo at `ref: main` and runs `_workflow-templates/scripts/kuma_sync.py multi` directly (contrast INF-5, which was inert behind the collection SHA pin). **The acceptance test therefore self-serves:** kuma-sync is scheduled `0 */6 * * *`, so two consecutive scheduled runs within ~12 h satisfy the WO's criterion with no dispatch. Expected signature: the FIRST post-fix run should already report `updated: 0` with every monitor `unchanged`, unless something genuinely drifted — anything else means the permanent diff was not fully closed, and the new scope-0 `diff on '<key>'` log lines will name the remaining key. **Gate #2 re-review (operator-approved, 2026-08-06): no new P1; R1/R3/R5 confirmed genuinely fixed; four follow-ups raised, all now closed.** F1 (P2): the R1 fix has TWO guards and only the first was ever regression-tested — removing the recovery-branch guard left all 30 tests green. My earlier claim of a non-tautological check covered guard (a) only; that was an overstatement. A first attempt at the missing test did not reach the guard either, because `_retry_call` reconnects on its own, so a budget armed by reconnect fires during the write's retries and never gets to the recovery branch; arming on the SECOND `get_monitors` pins the re-fetch exactly. Both guards are now individually verified: removing (a) fails 2 tests, removing (b) fails 1. F2 (P2): `test_multi_budget_abort_skips_the_prune` was tautological — a mis-caught BudgetExceeded still lands in `failed_apps`, and that alone already skips the prune via the PRE-EXISTING safety net, so the test passed with the guard removed. Strengthened to assert the abort was identified AS a budget abort; it now fails without the guard. F3 (P3): my stated rationale for deleting the `bool` branch was WRONG — `_build_monitor_kwargs` whitelists KEYS, not value types, and PyYAML resolves an unquoted `keyword: off` in `monitoring.extra` to Python `False` (verified directly), so the branch was reachable. The deletion stands for the correct reason: `bool` is an `int` subclass and has always fallen through the int branch, while the added branch silently changed that (`bool("true")` is True where `int("true")` raises). Pinned by 5 new cases instead of an inaccurate comment. F4 (P3): a budget abort during the prune could previously only fire reactively from inside a failed delete's retry, stopping at an arbitrary point mid-prune; added a proactive `session.budget.check()` before each delete so the stop is deterministic. Deletions already made stand — they only ever remove monitors absent from the declared set, so nothing valid is lost. **Tests now 37** (was 30). Reviewer confirmed the tree was left clean after its own guard-removal experiments.|
| TS-1 | Tailnet join resilience | staging-health raw join → tailnet-connect probe-guard; `use-cache` + re-probe-gated retry on cold joins; internal composite refs → v2.5.2 | 2026-07-16 | done | 67ba66f | Phase 1 on `main` (10 files), reviewed independently, no findings; tag `v2.5.2` = `2c59eab` (register commit on top of `67ba66f`, self-refs verified). Phase 2 = webapp-management `CI-1` (`a312525` + `0c581ef`, 8 pins); first live cold-join green with cache seeded (deploy-traefik run 29522808584: probe false → CDN download → "Cache saved … tailscale-1.98.4", no "Unexpected input" warning — subsequent runs restore from actions/cache). Guardrail: never `tailscale reset` / unconditional `up` on the shared daemon (regression `40c70c4`, known-good `e196e6c`). deploy-app deliberately NOT converted (pinned `@v2.4.4` by app repos; convert next time it is touched) |
| CI-1 | Unique per-run CI image tag | app-ci backend job: replace the fixed `ci-backend:test` with `ci-backend:${{ github.run_id }}-${{ github.run_attempt }}` in build-push tags AND the `docker run` pytest step, plus an `if: always()` `docker rmi` cleanup step — the fixed tag races between concurrent backend jobs on the shared daemon and executes the WRONG repo's tests | 2026-07-17 | done | 53e862c | Proven cross-repo execution: jg-ferien PR #71 (run 28984827793, 00:16:39Z) collected hram's `runs/tests/test_golden_regression.py`, hram CI started 34s later; hram develop CI red 07-15/16 was MIXED: the 08:40 run executed survey_app's `surveys/test_ws_inventory.py` (image race), while runs after hram's 07:13 engine-v1.12.0 bump (`4c0e6ea0`) also fail on hram's OWN `calculate/` tests — a real regression tracked separately in hram; post-fix hram runs stay red on OWN tests until that lands. app-ci is consumed `@main` → fix is fleet-live on push. Companion issue (app repos, separate WO): jg-ferien test_settings lacks CHANNEL_LAYERS/CACHES overrides (dcm 2.25 defaults → `redis://redis` unresolvable under `--network host`). Residual assumption (implementer+reviewer+orchestrator concur): tag uniqueness relies on NO matrix on the backend job — verified for all 13 current callers; a future matrix must add `strategy.job-index` to `CI_IMAGE`. Orchestrator post-landing review 2026-07-17: clean, no findings |
| CI-2 | Maintenance action: no false failure on reboot / apt sshd restart | The `maintenance` composite action (`.github/actions/maintenance/action.yml`) red-flags a run (exit 255, "connection closed by remote host") when a target box legitimately reboots or when `apt-get upgrade` restarts sshd mid-session — the bare `ssh` calls in the apt step (`if apply_updates`) and the decide step (trailing `ssh "uname -a; uptime"`) run under `set -euo pipefail` with no reconnect tolerance. Make the run finish GREEN when the box returns healthy with expected containers present; red only on genuine problems (box never returns within the wait timeout, expected container tokens missing, real apt error). | 2026-07-19 | done | 07897d5 | Root cause of the recurring webapp-management "production deploy failed" mail (cockpit mislabels any failed main workflow). Evidence: webapp-management run 29676082177 (2026-07-19 06:13 UTC), all 3 prod targets X on the maintenance step, `Permanently added to known hosts` → `Connection closed by remote host` → exit 255 (early ssh). Code fix landed on `main` (`07897d5`): apt upgrade decoupled from the SSH session (detached `setsid nohup` + status-dir marker file, polled by a new reconnect-tolerant "Wait for system updates to finish" step); decide-step's trailing informational `uname -a; uptime` hardened with `\|\| true`. Independent review: found + fixed a P1 (apt-run exit code was captured from the trailing group command, not the actual failing command — masked real apt failures as green; fixed via an inner `( set -e; … )` subshell with its own captured `$?`), re-reviewed and confirmed fixed. Tagged `v2.5.3` (= `286ac59`, dereferenced-verified on remote); webapp-management pin bumped `@v2.5.2` → `@v2.5.3` (`f9561ae`, one-line diff independently reviewed, no other stray pin found, input contract unchanged). Staging registry had no `maintenance` role (only `*-prod` boxes did) — temporarily added (`6ed4eb3`), live-dispatched (`gh workflow run maintenance.yml --field target=staging --field apply_updates=true --field force_reboot=false`, run `29682171283`), reverted after (`cb791e1`). Live run fully exercised the fix end-to-end in one go: staging genuinely had 25 upgradable packages incl. a kernel bump → apt ran backgrounded through the actual sshd restart (no false-red) → `reboot-required` detected → reboot triggered → host reconnect-waited successfully → post-check found all expected containers — run concluded green. `force_reboot=true` dry-run skipped as redundant (reboot/wait/post-check path was already proven live by the natural reboot). Prod runs next hit this fix on the regular Sunday 01:00 UTC cron (2026-07-26) or on-demand dispatch. |
| CI-3 | Shift-left secret-scan + dep-audit gate | New `security` job in app-ci.yml (reusable `workflow_call`, fleet-wide via `run-security` input, default true): blocking gitleaks secret scan + pip-audit/pnpm-audit report-only (continue-on-error). Companion pre-commit config (gitleaks + detect-secrets) shipped to webapp-template + piloted in survey_app (separate WOs there). | 2026-07-29 | done | 7fbbb42, 33ac3ce | reviewer + sec_reviewer: no open findings after fixes. Live `workflow_dispatch` validation on webapp-template caught 2 real defects missed by static review, both fixed + re-validated live before relying on this fleet-wide: (1) `gitleaks/gitleaks-action` now requires a paid `GITLEAKS_LICENSE` for GitHub orgs (breaking upstream change) — replaced with a direct, SHA256-verified download of the free OSS `gitleaks` CLI; (2) sec_reviewer S1: the replacement's first cut (`gitleaks dir .`, working-tree only) would miss a secret added then removed within the same PR (still lands permanently in merged history/blame) — empirically confirmed the gap with a synthetic add-then-remove-secret test, then fixed to `gitleaks git . --log-opts=<PR base..head, or HEAD~1..HEAD on non-PR triggers>`. Final live re-dispatch (run 30450585006): security/backend/frontend all green. |
| INF-5 | Stop the provisioning play from upgrading the transport it runs over | Collection half of webapp-management's INF-5 (same WO, two repos). The `base` role's "Apt update and upgrade" task upgraded the `tailscale` package; its postinst restarts `tailscaled`, which serves the Tailscale-SSH session Ansible connects through, so the play terminated its own connection and every first `ansible-provision` run against a host failed `UNREACHABLE`. New `base_apt_hold_packages` (default `[tailscale]`): dpkg-held across the apt upgrade, released in `always`, only for installed packages. Upgrade path unchanged — the weekly `maintenance` workflow and the runner's local os-maintenance timer both run apt detached and tolerate the session drop. Full order: [`webapp-management/work-orders/INF-5.md`](https://github.com/bigler-webapps/webapp-management/blob/main/work-orders/INF-5.md) | 2026-08-06 | done | 43b4f41 | Tier 2, prod-infra. Consumed by SHA pin (`webapp-management/ansible/requirements.yml`), so this change is inert until that pin is bumped. galaxy version 0.5.1 -> 0.5.2 (patch: behaviour fix to an existing role plus one knob, not a new capability area). Landed 2026-08-06 together with the consumer half (webapp-management d41023a, which bumps the pin f91f71a->43b4f41 — this change is inert until that bump). Codex was out of workspace credits on both chunks, so the Codex->Claude fallback applied and an independent `reviewer` was mandatory: no P1, one P2 (R1) FIXED — `always` cannot run if the Ansible controller dies (CI cancellation, runner crash), which would leave a real dpkg hold behind that BOTH designated upgrade paths respect silently; closed by arming a host-side `systemd-run --on-active=60min /usr/bin/apt-mark unhold <pkg>` before anything can fail, so the worst case is bounded at one hour instead of indefinite. Anonymous unit (no `--unit`) so back-to-back provisions cannot collide, never disarmed because a later firing is a no-op on an unheld package. 7 structural pytest cases in `ansible/roles/base/tests/` (hold precedes the upgrade, release sits under `always`, hold and release cover the same set, only installed packages are held, safety net armed before the upgrade). No live Ansible verification — no ansible/ansible-lint on the maintainer machine; the acceptance test is an operator-gated `ansible-provision` against a host with a PENDING tailscale upgrade. **Live smoke run on staging 2026-08-06** (webapp-management run 31094650853): green first attempt, `ok=109 changed=6 unreachable=0 failed=0`. All six new/changed tasks executed in order; `apt-mark showhold` empty afterwards; the anonymous safety-net timer armed with ExecStart `/usr/bin/apt-mark unhold tailscale`; `tailscaled` start time unchanged from the 2026-08-05 incident, i.e. the play no longer restarts its own transport. Runtime deps confirmed present on a real host (python3-apt importable, systemd-run, apt-mark). NOT the acceptance test — staging had no pending tailscale upgrade, so the hold was not load-bearing in this run.|
| INF-8 | Give Kuma an alert channel a human actually reads, independent of cockpit | Collection half of webapp-management's INF-8 (same WO, two repos). `sync-kuma-notifications`'s composite action gains seven optional `smtp_*` inputs (host/port/secure/username/password/from/to), each exposed to `kuma_sync.py notifications` as the matching `SMTP_*` env var — same contract as the existing `discord_webhook_url` -> `${DISCORD_WEBHOOK_URL}` mapping. All optional, empty default, no behaviour change for an existing caller. Full order: [`webapp-management/work-orders/INF-8.md`](https://github.com/bigler-webapps/webapp-management/blob/main/work-orders/INF-8.md) | 2026-08-06 | done | 640f448 | Tier 2, prod-infra (new secret material, alert routing). This half is inert on its own — the consumer (webapp-management) wires the inputs from `secrets.yaml` + `notifications.yml` and pins to this SHA, same pin-then-consume shape as INF-5. Exact Kuma SMTP field names verified against the installed `uptime-kuma-api-v2` 1.0.3 client (`notification_provider_options`), not guessed: required `smtpHost`/`smtpPort`/`smtpFrom`; `smtpSecure`'s accepted string values are NOT established by the client (free `str`, no enum) and must be confirmed against a live Kuma instance before relying on it — left unset in the consumer wiring pending that confirmation. **Landed 2026-08-06** together with the consumer half (webapp-management, which bumps the pin @v2.5.2->@v2.6.0 in the SAME commit as its own wiring — separating them would have let a merged state exist with the wiring but not the inputs, silently creating a broken Kuma notification with unexpanded ${SMTP_*} placeholders). Tagged v2.6.0 = 640f448 (dereferenced-verified on remote), which also carries CI-4 and CI-5. Codex was out of workspace credits (7th consecutive failure this session) before writing anything, so the Codex->Claude fallback applied and an independent `reviewer` was mandatory: no P1, two P2, two P3, all fixed. P2 (test coverage): the "no literal secret" test only checked placeholder PRESENCE, not the absence of a real credential elsewhere in the same block — rewritten to scan every smtp-prefixed line in the whole file and assert its value is exactly one of the six documented placeholders; verified non-tautological by injecting a fake literal and confirming the new test catches it. P2 (the pin-bump gap, closed by tagging as above). P3s: the discord-alerts transition comment overclaimed Kuma’s dual-default behaviour as fact -- corrected, and in fact STRENGTHENED once CI-5 landed concurrently (see that row): the sync itself now explicitly resolves and attaches default notification ids rather than relying on unverified Kuma UI behaviour. The open-ended dual-default transition window (P3, judgment call) is tracked via the WO register’s Orchestrator-only steps, same mechanism used for every other multi-dispatch WO this session -- accepted, not code-enforced. Tests: 24 green (scripts/tests/test_sync_kuma_notifications_action.py).|
| CI-9 | Build the image once on the runner, pull it on the server | `docker push` and `ghcr.io` appear **nowhere** in `app-ci.yml` or `deploy-app/action.yml`, so the image is built **twice** per change: the runner builds it, tests against it and discards it (`app-ci.yml:~138`), then the deploy builds the same image again on the target (`deploy-app/action.yml:551`, `compose up -d --build`). Fix: CI logs in to ghcr.io, tags by commit SHA and pushes; the deploy logs in, pulls, and runs `up -d` without `--build`, with `--build` retained as an explicit fallback. Full order [`work-orders/CI-9.md`](work-orders/CI-9.md) | 2026-08-08 | done | workflow-templates@7c4f419 | Tier 2, prod-infra. Not a considered design with a tradeoff — it is the default of `compose up --build` and the push/pull half was simply never written; the `image_name: ghcr.io/…` in every `project.yaml` and the `image:` lines in every compose file are decoration today, since nothing pulls from there. **Measured consequence:** main-prod's build cache alone was 40 GB on 2026-08-08, plus BuildKit, intermediate layers and the CPU/RAM cost of building during each deploy, on a box hosting twelve apps. **Favourable starting point:** every compose file already declares `image: ${IMAGE_NAME}:${IMAGE_TAG}` AND `build:` on the same service — exactly the dual-mode shape — and `generate-env` (dcm ≥2.17.5) already emits `IMAGE_NAME`, so **no app repository changes**. If the implementation starts editing compose files, that assumption is wrong and it is a scope change back to the operator. **Net security improvement:** one new scoped GHCR read token on the servers, against the removal of `HRAM_ENGINE_READ_TOKEN` (BuildKit secret, `action.yml:78`) and `VITE_APP_MUI_LICENSE_KEY` from every prod box — verify the removal, do not assume it. **CI-6 interaction:** repos using `backend-target: backend_test` build a leaner test-only image and need the full image built too for the push (cheap on the runner's warm cache, replaces a full build on the server). **Sharpest risk is tag discipline** — `:latest` as the deploy tag would discard the determinism a server-side build gives for free; a silently-latest deploy looks like it worked and ships an unknown image, hence the fail-loud-on-unresolved-`IMAGE_TAG` requirement. ghcr.io becomes a deploy dependency (hence the retained fallback). Interim measure meanwhile: `webapp-management` **INF-11** (daily build-cache prune) — must land independently, this WO must not block it. Implemented via Codex; independent review: `reviewer` (Sonnet) — one P1 (reusable-workflow `GITHUB_TOKEN` permissions are capped by the caller, so `packages: write` in `app-ci.yml` alone can't push; every app repo's `ci.yml` grants only `contents: read`), fixed by switching to a single new org secret `GHCR_TOKEN` (write:packages, covers push AND pull — fewer credentials than the Envelope's original two-mechanism plan, still zero app-repo changes since every caller already uses `secrets: inherit`); P2 (docker compose pull on build-only services, e.g. hram's `java_backend`) verified empirically as a graceful skip, not a failure; P3 (over-broad `permissions:` scope) resolved as a side effect of the P1 fix. Scoped tests green after the fix. Operator must still provision `GHCR_TOKEN` in Proton/GitHub Org Secrets before staging validation; rollout/first pull-deploy remains operator-gated. **SUPERSEDED on the secrets question by CI-10 (2026-08-08): do NOT provision `GHCR_TOKEN`.** `secrets.yaml` has no org scope, so an org secret could never be managed by `sync-secrets`, covered by the rotation reminder, or recorded in Proton — and the "zero app-repo changes" that justified it does not hold, since the in-chain alternative would need a declaration per app repo anyway. CI-10 uses the per-run `GITHUB_TOKEN` with `packages: write` granted by each caller: no credential to create at all. This WO's transport design (SHA tags, `--build` fallback, `compose pull`, the `.env` token stripping) stands unchanged. |
| CI-10 | Publish to GHCR with the built-in `GITHUB_TOKEN`, not a new org secret | Replaces CI-9's `GHCR_TOKEN` org secret with the per-run `GITHUB_TOKEN`. CI-9's reviewer correctly found the P1 — GitHub caps a reusable workflow's token at the caller's grant, and every app `ci.yml` grants only `contents: read` (verified: `cockpit/.github/workflows/ci.yml:8-9`) — but priced the remedy wrong. `webapp-management/secrets.yaml` has **no org scope** (only `repo`/`env`), so `sync-secrets` cannot manage an org secret at all: it would be hand-created in the GitHub UI, with no Proton entry, no `secret-rotation-reminder.yml` coverage and no record of scope. Following the estate's documented pattern instead (fine-grained PAT in Proton, `target_scope: repo`, like `ROUTINES_GH_PAT`) would need a declaration in EACH app repo, because `secrets: inherit` resolves against the calling repo. Both principled options cost ~12 small per-repo edits, so prefer the one that adds **no credential at all**. Scope: `GITHUB_TOKEN` for login in `app-ci.yml` + `deploy-app`, the `org.opencontainers.image.source` label in the SHARED build command (no Dockerfile touches — none sets it today), and `packages: write` in each app's `permissions:` block. Full order [`work-orders/CI-10.md`](work-orders/CI-10.md) | 2026-08-08 | in-progress | workflow-templates@6f58517, cockpit@1e32b31 | Tier 2, prod-infra. **Verified precondition: the org's container package list is EMPTY** (operator-confirmed 2026-08-08, consistent with CI-9's finding that `docker push` appeared nowhere before it). That settles the open question — the first push with `packages: write` CREATES the package and grants the pushing repo admin automatically, so **no manual package-permission step is needed**. The case that would have required an admin (an existing unlinked package) does not apply. **Prototype: cockpit**, operator-chosen, and it fits CI-9's "staging first" without exception — `project.yaml` declares a `staging` environment on the `staging` server (`staging-cockpit.bigler-consult.ch`), separate from production on main-prod, so a failed first pull-deploy touches only the staging box. It is also the HARDEST pull on the estate (largest image, spaCy ~2 GB, see cockpit IMG-1), which makes it a genuine stress test rather than a convenient one. Production follows only after staging proves out. **Coordinated rollout, not one commit:** every `@main`-pinned app (cockpit, hram, jg-ferien, survey_app; spesix pins a SHA and is unaffected) currently fails at CI-9's publish step for want of `GHCR_TOKEN`, and landing the shared half alone does not fix them — each repo stays broken until it grants `packages: write`. Explicitly do NOT "fix" that by skipping publish when permission is absent: a silently skipped publish yields a deploy pulling a tag that was never pushed. CI-9's transport design (SHA tags, `--build` fallback, `compose pull`) stands unchanged; only the credential moves. **Landed so far: `workflow-templates` shared half + cockpit only** (per Execution directive — stop after the prototype). Implemented via Codex; independent review: `reviewer` (Sonnet) — one P1 that traced back to the WO's own Implementation map: `deploy-app/action.yml` is a composite action with NO access to the `secrets` context (unlike a reusable workflow) — the map's instruction to reference `secrets.GITHUB_TOKEN` directly inside it is invalid GitHub Actions syntax and would fail the entire action invocation, not just the login step. Confirmed against the file's own 100%-consistent prior convention (`hram_engine_token`, `secrets_context_json`, `ts_oauth_*` are all declared `inputs:`, zero prior `secrets.*` references anywhere in the file). Fixed by adding a proper `github_token` input, wired from cockpit's `main.yml` `with:` block; a regression test (`test_deploy_action_never_reads_secrets_context_directly`) now asserts no composite action in this repo references `secrets.*` directly. All 7 scoped tests green after the fix. **Still open before this WO can close:** (1) operator must cut a new `workflow-templates` release tag (not done autonomously — tagging is outside this session's allowed scope) and bump cockpit's `deploy-app@v2.4.4` pin before the prototype deploy can exercise any of this; (2) the actual staging-cockpit pull-deploy acceptance run is unverified — no deploy was dispatched; (3) after the prototype proves out, hram/jg-ferien/survey_app still need the same `packages: write` + `github_token` wiring (spesix pins a SHA, unaffected until its pin is bumped). |
| CI-11 | Publish the image in the deploy workflow, so the pull has something to pull | CI-9/CI-10 built a correct pull path on an assumption that does not hold here: that CI runs before a deploy. Verified triggers — `ci.yml` is `on: pull_request: [develop, main]` + `workflow_dispatch` with **no push trigger**, `main.yml` is `on: push: ['develop','main']`. The two react to **disjoint events**, and this estate commits DIRECTLY to `develop` per `AGENTS.md` (no feature branches), so a push fires only the deploy and never CI. Fix: publish in the SAME job that deploys, as a step before `deploy-app` — `main.yml`'s deploy job already runs `runs-on: [self-hosted, netcup]` (the same warm-cache runner CI builds on), already declares `packages: write` and already passes `GITHUB_TOKEN`, so it needs a step, not a job: no `needs:`, no `workflow_run`, no cross-workflow race, and the existing `concurrency` group covers it. Full order [`work-orders/CI-11.md`](work-orders/CI-11.md) | 2026-08-08 | done | workflow-templates@1efa2e0 (tag v2.9.0), cockpit@efe3862, hram@062700e4, jg-ferien@4a89e83, survey_app@d44592e | Tier 2, prod-infra. **Observed live 2026-08-08** across cockpit, hram, jg-ferien, survey_app: GHCR login succeeded (CI-10's auth fix is correct), `docker compose pull` then found no image for the SHA. All four staging deploys failed identically and **harmlessly** — every container kept running on its previous image, no outage, no rollback. Operator decided against rolling back the pins; the four stay red on every push until their `main.yml` carries the step. **Both CI-9's and CI-10's reviews missed this** because both examined the workflow files in isolation and never checked which events reach them — the execution directive therefore tells CI-11's reviewer to check the trigger topology explicitly. **Real defect folded in (scope item 3):** `main.yml`'s `workflow_dispatch` path checks out a DIFFERENT ref (`staging→develop`, `production→main`) while CI-10's publish step tags with `github.sha`, the *triggering* ref — on a dispatch those diverge and you build one commit but deploy under another's tag: green, silent, wrong. Resolve the SHA once after checkout for both. **Named non-goal:** staging deploys must NOT become test-gated — a push to `develop` deploys immediately and untested by deliberate design (staging is the test bed). Publishing is a deploy prerequisite, running tests is a quality gate; coupling them is why "deploy calls `app-ci.yml`" was rejected, and it is pinned as a structural assertion so a later refactor trips over it. **Landed so far: shared half + cockpit** (per Execution directive — stop after the prototype). Implemented via Codex; independent review (Sonnet), explicitly instructed to check the trigger topology per this WO's own directive: no findings — confirmed same-job step ordering eliminates the race, confirmed `git rev-parse HEAD` after checkout is correct where `github.sha` was wrong, confirmed the `existing_image`/`backend_target` branch logic covers all three real call shapes, confirmed zero `secrets.*` references in the new composite action (the CI-10-class bug). 8 scoped tests green. **Live verification attempted twice (push + workflow_dispatch), both blocked by an unrelated pre-existing issue**: `docker build` fails pulling `ghcr.io/astral-sh/uv:latest` from GHCR ("failed to fetch oauth token: denied: denied") — confirmed NOT a CI-11 regression, since the identical error also occurs on `main`'s untouched `deploy-app@v2.4.4` path (the old server-side `--build`, never touched by this WO). Staging stayed healthy both times (non-destructive failures, existing containers kept running). **Not yet rolled to hram/jg-ferien/survey_app** — blocked on a clean live run first, per the WO's own stop-gate; the `ghcr.io/astral-sh/uv` pull issue needs resolving (or waiting out, if transient) before that can happen. **ADDENDUM 2026-08-09 — the pull issue was neither external nor transient: the registry login leaks into SHARED credential stores.** A public GHCR image needs no auth; `failed to fetch oauth token: denied` means the daemon PRESENTED a credential and the exchange was refused for that scope — the repo-scoped `GITHUB_TOKEN` from CI-10/CI-11, left behind because neither side logs out. **Runner:** `publish-backend-image/action.yml:79` logs in; the three ephemeral `gh-runner@1..3` slots share one home `/opt/gh-runner`, whose config.json was confirmed to list `auths: ['ghcr.io']`. **Server:** `deploy-app/action.yml:595` logs in INSIDE the SSH block, i.e. on the target host. Both explain why the old unchanged `v2.4.4` path failed too — it never logs in, it INHERITS. **Blast radius was wider than the four migrated repos:** `fitness-monitor`, never part of the migration and still building server-side, was broken because it shares a host with apps whose new-path deploy had logged in there — on any box that saw a new-path deploy, server-side builds were broken for EVERY app on it. **Resolved 2026-08-09** by `docker logout ghcr.io` as the owning user on the runner, staging, and preventively contact-prod + innoservice-prod (main-prod carried no entry); fitness-monitor's deploy went green immediately. **Added scope 5, and CI-11 is not done until it lands:** scope `DOCKER_CONFIG` to a job-owned temp dir on BOTH sides (runner action, and inside deploy-app's SSH block). Scoping, NOT an `always()` logout — a job or SSH session dying between login and cleanup still leaks, and with three parallel slots and twelve apps per host that is the observed state, not a corner case. **Scope 5 landed 2026-08-09** (`workflow-templates@742ec06`): `DOCKER_CONFIG` scoped to a `mktemp -d` job/session-local directory in both `publish-backend-image` (before the build, not just before login — the build itself can read a stale shared config) and `deploy-app`'s SSH block (before the `if/else` branch point, covering both the pull path and the `--build` fallback), each removed via `trap ... EXIT`. No `docker logout` added (explicitly ruled out — leaks whenever the job/session dies between login and cleanup). Implemented via Codex (first attempt correctly stopped on a self-contradictory prompt — asked for "two files only" while also requiring new tests in a third; re-run with clarified scope). Independent review (Sonnet): one P2 — the SSH block's `sudo -n docker` fallback (used only when the deploy user isn't in the `docker` group) resets the environment by default and could silently drop the `DOCKER_CONFIG` scoping; fixed with `--preserve-env=DOCKER_CONFIG`, plus a regression test. One P3 — the fallback branch's export-ordering wasn't separately asserted; added. 11 scoped tests green. **Live-verified end-to-end 2026-08-08** (tag `v2.9.0`, `cockpit@efe3862`): push → publish step logs in and pushes `ghcr.io/bigler-webapps/cockpit-backend:efe3862ed3e9918a76a37ed7a7f9856d891a81c5` → deploy step pulls that EXACT tag and recreates containers, no build on the target — confirmed via the run log and `docker ps` on staging showing `backend`/`worker` running that exact image, seconds old. Full acceptance criterion met: same-job sequencing, correct SHA (matches the triggering commit, no divergence), pull not build. **Rolled to the remaining three repos 2026-08-08** (`hram@062700e4`, `jg-ferien@4a89e83`, `survey_app@d44592e`, all pinned to `v2.9.0`) — all three deploys went green on the first push, confirmed via `docker ps` on staging showing freshly-recreated, healthy containers. **CI-9/CI-10/CI-11 workstream complete**: build once on the runner (CI-9), the built-in per-run token with no stored credential (CI-10), publish in the same job that deploys with the credential store scoped to that job/session (CI-11). |
| CI-12 | Roll the publish-then-pull deploy to the remaining app repos | Four repos were already live on `v2.9.0`; rolled to eleven more plus a twelfth discovered mid-WO (fitness-monitor). Per repo: `packages: write` in `ci.yml` AND `main.yml`, the publish step before `deploy-app` in `main.yml`, both pins to `v2.9.0`. Full order [`work-orders/CI-12.md`](work-orders/CI-12.md) | 2026-08-09 | done | workflow-templates@(WO doc only, no code change) · bigler-consult@896cb6c · innoservice@76aefde · kerzenziehen@06597bc · reimbursements@3abf5dc · spesix@46471cf · survey_contact_app@851db14 · webapp-template@ce0a035 · yopoulab-hhs-2026@3654308 · hpc-bridge@cdfb0b3 · webshop-guenter@c6ac51a · fitness-monitor@ebc8645 | Tier 2 — **informed deviation**: `deps/CI` is a Tier-3 entry criterion, operator classified Tier 2 on 2026-08-09 as mechanical repetition of a pattern verified three times, no new design; implemented directly (not via Codex) on that same reasoning. **Scope corrected mid-WO — Kira excluded:** the originally-named eleventh repo, Kira, is NOT part of this estate (`git remote -v` → `mhaas02/Kira`, a different GitHub org; runner label `hetzner` not `netcup`; domain `km-h.ch`; image namespace `ghcr.io/mhaas02/`). Operator caught this after the change had already landed on it (Kira's `ci.yml` uses a non-default `build-context: backend` which the mechanical copy hadn't matched, causing a build failure that surfaced the repo during triage). Fully reverted via `git revert --no-edit` + push (commit `e62f989`), never force-push/reset. **Missing repo found and added:** operator identified a 13th real app not in the original repo list — `fitness-monitor` (cloned fresh, standard shape, old pin `v2.6.0`); `survey-routing` was also raised but the operator confirmed it is archived and explicitly excluded. **Four non-uniform cases, each handled deliberately:** spesix pins `app-ci.yml` to SHA `a114d6e` (left unchanged, no `ci.yml` change there); yopoulab-hhs-2026 has no `ci.yml` and no backend image at all (`image_name: caddy:2-alpine`) — deliberately given NO publish step, only the pin bump + `packages: write` + `github_token`, confirmed live in 22s pulling `caddy:2-alpine` cleanly; webshop-guenter has a `ci.yml` but no `app-ci` pin (left unwired, asserted by a dedicated regression test); webapp-template's deploy job is skipped by the `is_template` guard, so it is **bumped but unverifiable by construction — not counted as a verified green**. **Bug found post-review, in ALL twelve repos:** `deploy-app`'s own `with:` block was missing `github_token` (a separate input from `publish-backend-image`'s, needed for the remote GHCR login on the server side) — surfaced only when fitness-monitor's live deploy failed with `GITHUB_TOKEN is required for pull-based deploys`; confirmed via `grep -c` that all twelve repos had the same gap. This had passed both my own review and the independent Sonnet reviewer undetected (neither test suite asserted the string's count in `deploy-app`'s block specifically). Fixed with a separate commit per repo (`fix(CI-12): pass github_token to deploy-app...`, never amended) plus a new regression test (`test_both_publish_and_deploy_receive_github_token` / `test_deploy_receives_github_token`) closing the gap for future rollouts. Independent review: `reviewer` (Sonnet), spawned across all twelve diffs — one P3 (thin test coverage), did not catch the `github_token` gap either (same blind spot as the first-pass self-review). **Live-verified green** (push + `gh run watch`, staggered per the shared 3-slot netcup runner): fitness-monitor, bigler-consult, hpc-bridge, webshop-guenter, spesix, yopoulab-hhs-2026, kerzenziehen, survey_contact_app, innoservice. **reimbursements initially failed, but NOT on this WO's change and now resolved:** the publish/pull/token mechanics succeeded; it failed at `manage.py migrate` with `django.***.utils.NotSupportedError: PostgreSQL 15 or later is required (found 14.1)`. Root-caused as a separate dependency-pin defect (see reimbursements@fc5627a below), fixed and live-verified green (54s) — no DB engine change was needed after all; the operator-approved diagnosis ruled out a Postgres upgrade once the actual cause was found. Drive-by, not part of the eleven: `survey_app` (already on `v2.9.0` from CI-11) was missing `vite_app_mui_license_key` on its publish step despite the Dockerfile declaring that ARG — added. **CI-9/CI-10/CI-11/CI-12 workstream now covers all live apps except webapp-template's by-construction gap.** |

| CI-12-followup | reimbursements' `Django>=6.0.6,<7.0` resolved Django 6.1, which raised the DB engine floor to PostgreSQL 15 | Verified across every app repo before touching anything: `postgis/postgis:14-3.1` is uniform across all 13 apps, so the DB was never the actual variable — reimbursements was the only repo with an unbounded major-version range. Sibling apps with direct Django pins are all safely capped below 6.1 (hram `>=6.0.7,<6.1`, innoservice `==6.0.6`, jg-ferien/survey_app/survey_contact_app `==6.0.7`, kerzenziehen `==6.0.5`); the `django-core-micha`-based apps (bigler-consult, cockpit, fitness-monitor, hpc-bridge, spesix, webapp-template, webshop-guenter) are likewise protected by dcm's own `Django>=6.0.5,<6.1` cap (`django-core-micha` pyproject.toml, v2.41.2). Confirmed via the failed run's build log: `+ django==6.1`. Fix: pinned reimbursements to `Django==6.0.7`, matching the exact patch already running successfully on three sibling apps — no PostgreSQL upgrade needed. Full order [`work-orders/CI-12.md`](work-orders/CI-12.md), folded into the same entry (no separate file — trivial one-line dependency fix) | 2026-08-09 | done | reimbursements@fc5627a | Tier 2 — operator explicitly approved as a dependency-surface change, then explicitly scoped to "reimbursements only," declining the broader option of also bumping kerzenziehen (`6.0.5`) and innoservice (`6.0.6`) to `6.0.7` for full uniformity across the estate — those two stay on their current safe, working pins; no acute reason to touch them. Live-verified green (`gh run watch`, 54s) immediately after push. |
| CI-13 | `app-ci.yml`'s local composite reference resolved against the caller's checkout, not workflow-templates | Operator relayed a CI failure analysis from another session (two PRs, hram + hpc-bridge, both failing "ci / backend" with `Can't find 'action.yml' ... under '.../.github/actions/publish-backend-image'`). Independently re-verified before acting: `app-ci.yml:112` checks out the CALLING repo at the workspace root (this is a `workflow_call` reusable workflow invoked cross-repo, e.g. `uses: bigler-webapps/workflow-templates/.github/workflows/app-ci.yml@main`); the local `uses: ./.github/actions/publish-backend-image` on line 185 therefore resolved against that checkout, which has no such directory. Silently broken since CI-10/CI-11 landed — `ci.yml` runs only on `pull_request`, and this estate commits directly to `develop`, so it went unnoticed until the first PR since then. **Blast radius wider than the two reported PRs: this blocks every `develop→main` promotion PR in the whole estate**, since `ci.yml` also fires on `pull_request` against `main`. Full order [`work-orders/CI-13.md`](work-orders/CI-13.md) | 2026-08-09 | done | workflow-templates@09cd1b0 | Tier 3 (CI/CD entry criterion), operator Gate-#0-approved after independent verification. **Surveyed all 4 workflow files in this repo before scoping the fix** (operator explicitly asked for this before granting Gate #0, having initially suspected "all or many workflows" were affected) — only `app-ci.yml` was broken; `restore-cross-server.yml` already uses the correct pattern (explicit `workflow-templates` checkout before any local composite ref); `staging-health.yml` has no local composite ref at all; `runner-maintenance.yml` isn't cross-repo. Also confirmed no composite action calls another composite via a local path (no nested version of this bug), and that CI-12's own deploy-time composite refs (`publish-backend-image@v2.9.0`, `deploy-app@v2.9.0` in every app's `main.yml`) already use the full cross-repo path, unaffected. Fix mirrors `restore-cross-server.yml`'s `dest_import`-job pattern: checkout `workflow-templates` into a named subfolder (`.wt-checkout`, pinned to the `v2.9.0` commit SHA) rather than the workspace root, since (unlike `restore-cross-server.yml`'s `source_export` job) this job already needs the caller's own checkout at the root for the Docker build. Implemented via Codex (first attempt, ran clean — no fallback needed). Independent review (Sonnet, Tier 3): no blocking findings — confirmed the composite's `run:` step resolves `project.yaml`/Dockerfile paths relative to `$GITHUB_WORKSPACE` (the caller's checkout), not `github.action_path`, so loading the action from a subfolder doesn't break its internal path resolution; confirmed the pinned SHA is a real ancestor commit (tagged `v2.9.0`) with a working `publish-backend-image/action.yml`; confirmed the new regression test's regex matches the file's actual indentation. Two P3s, both operator-accepted rather than fixed in-scope (proportionality — theoretical/pre-existing, fixing either would expand scope beyond this WO): (R1) no collision guard if a caller repo ever names a top-level file/dir `.wt-checkout`; (R2) neither the new `test_app_ci_composite_checkout.py` nor the pre-existing `test_deploy_app_action.py` is wired into any actual CI job — both are manual-only regression tests, a pre-existing gap this diff inherits rather than introduces. R2 tracked as its own row below (CI-14). 12/12 scoped tests green (`test_app_ci_composite_checkout.py` + `test_deploy_app_action.py`). No live PR re-run performed as part of this WO (the fix is structural/mechanical and the acceptance criterion — a real app-repo PR going green — depends on that app repo's own next PR, outside this repo's control); operator should confirm on the next actual PR in hram/hpc-bridge or any other app. |
| CI-14 | Wire `.github/scripts/test_*.py` structural regression tests into an actual CI job | Surfaced as CI-13's R2 finding: `test_app_ci_composite_checkout.py` and `test_deploy_app_action.py` are both manual-only — no workflow in this repo (`app-ci.yml`, `restore-cross-server.yml`, `runner-maintenance.yml`, `staging-health.yml`) invokes them. A future edit to `app-ci.yml`/`deploy-app`/`publish-backend-image` could silently break an assumed-passing regex with no CI signal. Not yet scoped or authored — no `work-orders/CI-14.md` exists yet | 2026-08-09 | planned | | Operator explicitly deferred fixing this alongside CI-13 (accepted the P3 rather than expanding that WO's scope) and asked for it to be tracked as its own entry instead. Needs a small design decision before authoring: a new lightweight `workflow-templates` CI job (`workflow_dispatch` + maybe `push` on `.github/**` changes in this repo, since this repo itself has no `develop` branch / PR-gated flow the way app repos do) that runs `python -m unittest discover .github/scripts`. Likely Tier 2 (new CI job, no sensitive surface) once scoped. |
| CI-15 | Fail CI on a duplicate i18n key, because JSON.parse never will | New blocking step in `app-ci.yml`'s `frontend` job: a per-object duplicate-aware JSON parse of each app's `translations.json` (both known layouts globbed), failing with the key, occurrence count, and every value. Found via `jg-ferien` PUB-9 (a duplicate `OrganizationPage.TEAM_ROLE` silently shipped the wrong label); an estate sweep found `innoservice` was the only other offender (five duplicates). Full order [`work-orders/CI-15.md`](work-orders/CI-15.md) | 2026-08-17 | done | 7f76a96, 352393f | Tier 3 (CI/CD, `app-ci.yml` consumed by 13 callers, most `@main` — no staged rollout). **Precondition satisfied first**: `innoservice` `I18N-1` (Tier 1, register row only) landed on `develop` (`30a1210`) before this merged, confirmed via a per-object duplicate check run directly against the post-fix file (0 duplicates). Implemented via Codex (`RESULT: DONE`, own 5 new tests green). **Independent `reviewer` (Sonnet): no P1.** One P2 — the new frontend-job self-checkout of this repo (needed to fetch the checker script into the caller's workspace) pinned `ref: main` instead of an exact SHA, inconsistent with the backend job's existing "pin + bump deliberately" convention for the same kind of self-checkout; the script couldn't be pinned in the SAME commit that introduces it (unknowable SHA), so landed as `main` then immediately fixed with a follow-up commit pinning to the landing SHA (`352393f`), mirroring the backend-job comment convention. One P3 (accepted, not fixed): the checker only globs the two known directory layouts (`i18n/translations.json`, `src/i18n/translations.json`) — an app using a third path would silently pass (fails open); explicitly in-scope per the Envelope's own estate sweep (twelve apps, both layouts covered), not expanded further. Reviewer independently verified the per-object `object_pairs_hook` cannot false-positive on the repeated `de`/`en`/`fr` nested keys across thousands of entries (traced against CPython's `json` semantics), and that the false-positive regression test is non-tautological. **Verification**: 5 new tests green; dry run against `jg-ferien/frontend/i18n/translations.json` (2067 keys, pulled to current `develop` first — an initial run against a stale local checkout falsely showed a duplicate that PUB-9 had already fixed remotely) passes clean, `innoservice/frontend/src/i18n/translations.json` passes clean post-`I18N-1`. |
| CI-15 | CI faellt bei einem doppelten i18n-Schluessel, weil `JSON.parse` es nie tut | Ein Schritt im `frontend`-Job von `app-ci.yml`, der die flache Uebersetzungsdatei einer App auf doppelte Schluessel prueft und blockierend fehlschlaegt, mit Nennung des Schluessels und beider Werte. Die Datei wird per Glob unter `frontend-path` gesucht, weil in der Estate zwei Layouts vorkommen (`i18n/` und `src/i18n/`). Vollstaendiger Auftrag [`work-orders/CI-15.md`](work-orders/CI-15.md) | 2026-08-17 | planned |  | Tier 3 (CI), kein Prototyp. **Vorbedingung: `innoservice` `I18N-1` zuerst**, sonst faellt eine lebende App bei diesem Merge rot aus fuer einen Defekt, den sie an dem Tag nicht eingefuehrt hat. Ausloeser war ein Zufallsfund in `jg-ferien` waehrend PUB-9: `OrganizationPage.TEAM_ROLE` war zweimal definiert, korrekt als "Rolle" frueh in der Datei und falsch als "Team" spaeter — `JSON.parse` behaelt den letzten, also gewann still der falsche. Gefunden wurde das beim Lesen, nicht durch eine Pruefung. Die anschliessende Messung ueber die Estate: **zwoelf Apps geprueft, elf sauber**, `innoservice` mit fuenf Duplikaten, vier davon wertgleich und eines lebendig (`Common.ConfirmDelete` ueberschreibt die englische und franzoesische Fassung). **Das schaerfste Risiko ist die Verbreitung, nicht die Pruefung selbst**: alle dreizehn Aufrufer haengen an `app-ci.yml@main`, ohne Version zum Zurueckhalten, also waere ein Fehlalarm nicht ein roter Build sondern dreizehn. Deshalb ist der Falsch-positiv-Fall ein Pflichttest und keine Kuer: verglichen werden duerfen ausschliesslich Schluessel der obersten Ebene, denn die verschachtelten Sprachschluessel `de`/`en`/`fr` wiederholen sich voellig regulaer in jedem einzelnen Eintrag, und eine naive Pruefung meldet alle 2067. Bewusst blockierend statt report-only, anders als die benachbarten `pnpm audit`/`pip-audit`-Schritte: die Pruefung ist deterministisch, hat nach korrekter Eingrenzung keine Fehlalarme, und die Behebung ist das Loeschen einer Zeile. **Envelope amended 2026-08-17 on operator instruction, by a second session, after re-measuring its claims.** All load-bearing facts held: `innoservice` five duplicated keys with `Common.ConfirmDelete` the one live divergence (`fr` and `en`), `jg-ferien` 2067 keys clean, only the two directory layouts across eight apps checked, and `@main` pinning confirmed on ten of ten apps checked — so the Tier-3 blast-radius argument stands. **One substantive change:** the section formerly titled "The trap that must be tested, not assumed" made the dot heuristic (`Section.KEY`) load-bearing and the false-positive case the most important test. That guards a hazard the correct implementation does not have — a duplicate-aware parse reports duplicates **per object**, and the nested `de`/`en`/`fr` keys repeat across *different* objects, never within one. Measured with no key-name heuristic at all: `jg-ferien` 0 duplicates, `innoservice` 6 occurrences across 5 distinct keys. Flattening into one namespace is the only thing that creates the false-positive case, and the dot rule then rests on two unenforced conventions (every section key contains a dot — 0 of 2067 without today; no nested key ever does — a `de-CH` locale tag would). Risk 1 reworded to point at the implementation choice rather than at the test, and the false-positive test kept as a regression pin against reintroducing flattening. **Two additions:** JSON is recorded as the only unguarded format (ESLint `no-dupe-keys` covers JS object literals and TypeScript rejects duplicates outright, so `ui-core-micha`'s `src/i18n/*Translations.ts` — the widest blast radius in the estate — is already covered), which is why the check is deliberately not widened; and the "duplicated three times" test is noted as non-hypothetical, `innoservice`'s `Common.Delete` being exactly that case. Not re-verified: the "twelve apps scanned, eleven clean" sweep — two files were measured, not the estate. Scope, tier, ordering precondition (`innoservice` `I18N-1` first) and review routing unchanged. |
