# WORK_ORDERS.md — workflow-templates

Work-order register for this repo. Lightweight directory (not the full orders):
one row per WO with its implementation status. Convention, schema, and maintenance
rules are defined centrally in `webapps/AGENTS.md` → "Work-Order Register".

**Scope note:** this register starts with the 2026-07 tailnet-resilience workstream;
older work is in `git log`.

## Workstream prefixes

| Prefix | Workstream |
|---|---|
| `TS-*` | Tailnet/Tailscale CI resilience (probe-guard, joins, CDN dependency) |
| `CI-*` | CI workflows / composite actions (non-tailnet) |
| `INF-*` | Shared with webapp-management — the `biglerconsult.infra` collection half of an INF work order. Same ID in both registers (one WO, two repos); the full order lives in `webapp-management/work-orders/`. |

Introduce a new prefix when none fits and add it here. New WOs always get a
prefixed ID; never reuse a bare flat number across workstreams.

## Register

| ID | Titel | Beschreibung | Datum | Status | Commit(s) | Notiz |
|---|---|---|---|---|---|---|
| CI-4 | kuma-sync rewrites every monitor on every run, and retries on a dead socket | `scripts/kuma_sync.py` reports EVERY monitor as changed on EVERY run, so each sync performs a full write pass of all ~57 monitors against Kuma's single-threaded Socket.IO server. `_retry_call` then retries five times (~50 s) on the SAME api object without reconnecting, so once the socket wedges every remaining attempt is a guaranteed failure. Fix: prove which key permanently differs (log it), normalise that comparison, reconnect between retries like `_login` already does, add an overall run budget, and add the tests this script currently has none of. Full order [`work-orders/CI-4.md`](work-orders/CI-4.md) | 2026-08-06 | done | 4800964 | Tier 2. Evidence from two consecutive runs 2026-08-06: failed run 31081103662 (07:29) = 39 updated / **0 unchanged** / 18 retry storms, 24 min, gave up on 4 apps (cockpit, hpc-bridge, innoservice, kerzenziehen) and correctly skipped the prune; passing run 31083824555 (08:10) = 57 updated / **0 unchanged** / 2 retry storms. Never a single `unchanged` in either. Prime suspect for the permanent diff: `notificationIDList` is sent as a list (`spec["notification_ids"] = [notif_id]`) while Kuma echoes that field as an object keyed by id, so `_monitor_changed` short-circuits on `not isinstance(current, list)`; second candidate `accepted_statuscodes`. Reasoned from the code, NOT proven — hence scope item 0 logs the differing key first. Retry signature confirms defect 2: attempt 1 `TimeoutError`, attempts 2-4 `UptimeKumaException`, repeated across 18 monitors, while `_login` (which DOES `api.disconnect()` between attempts) recovers fine. Failure rate 4/15 runs, denser since the monitor count grew to 57 (INF-2 +18, INF-3 -6). **Blocking:** INF-3's remaining operator-gated `kuma-sync` dispatch to remove the six `*-health-swap` monitors IS the run that failed at 07:29 — prune skipped, monitors still present, so INF-3 and webapp-management INF-2 cannot reach `done`. Interim option (not a fix): the workflow's `app_filter` input limits a run to `webapp-management` only. Acceptance criterion: two consecutive runs where the second reports `updated: 0` and all monitors `unchanged`. **Implemented 2026-08-06.** Codex was out of workspace credits, so the Codex->Claude fallback applied and this session implemented directly (independence flip: an independent `reviewer` was mandatory and was run). **The Envelope's prime suspect was WRONG, and scope item 0 is what caught it.** Two independent reasons `notificationIDList` cannot have been the permanent diff on the failing runs: (a) CI runs the `multi` subcommand, and `sync_monitors_multi` never resolves `notification_name` -> `notification_ids`, so the key is never sent and `_monitor_changed` (which iterates `kwargs` only) can never compare it; (b) the client library already converts that field dict->list[int] on read. **The real root cause is `type`:** `uptime_kuma_api` converts a monitor's `type` into a `MonitorType` enum ON READ while the script sends the plain string `http`, and the old comparison used `str(current)` — for a `(str, Enum)` member that is `MonitorType.HTTP`, not `http`. Every monitor, every run, forever, which is exactly the observed signature (0 `unchanged` in either evidence run). The old comment at that line claiming `str()` normalises enums was itself the bug. Confirmed against the client library source (`parse_monitor_type`), not guessed. Fix: `_unwrap()` compares Enum members by `.value`. `notificationIDList` is normalised to an id SET anyway (list/dict/dict-of-bool all equal) per scope 1, with `false` meaning NOT assigned so real un-assignment drift is still corrected (Risk 1). Scope 0's logging is retained permanently. **Scope 2:** the Envelope's `mirror _login's disconnect` is insufficient on its own — `_retry_call` received an already-BOUND method of the dead api object, so reconnecting inside it would still call the dead connection, and a local rebind could not reach the caller. Introduced a `Session` holder; retries resolve the method BY NAME against the live connection. All 11 call sites rewired; no third retry layer added (the existing `recovery_attempt` loop is unchanged and still re-fetches `existing`). **Scope 3:** `Budget`/`BudgetExceeded`, default 600 s via `KUMA_SYNC_BUDGET_SECONDS`, checked before each monitor and before each retry backoff; abort skips the prune (same safety as the failed_apps guard, which is untouched). **Independent `reviewer`: one P1 + one P2 + three P3, all fixed.** P1 (R1) was real and would have defeated scope 3 entirely: `BudgetExceeded` was swallowed by two generic `except Exception` handlers (the per-app `get_monitors` fetch and the recovery re-fetch), which rewrote it as a per-app failure and CONTINUED to the next app — so a wedged Kuma would still not fail fast, and would exit with a misleading `N app(s) failed` instead of the budget message. Fixed with explicit `except BudgetExceeded: raise` guards at both sites. P2 (R2): no integration-level test drove `sync_monitors_multi` through budget exhaustion — which is precisely why R1 survived unit testing; three such tests added, and the key one was verified NON-TAUTOLOGICAL by temporarily removing the guard and confirming it reproduces the exact misreport. P3s: `main()` disconnected the stale api rather than the session's live one after a reconnect (fixed); the dead `bool` branch logged un-normalised values (branch deleted as unreachable — `_build_monitor_kwargs` whitelists keys and none are boolean); `BudgetExceeded` surfaced as a raw traceback on the single-app path (handler added in `main()`). **Tests: 30 green** in `scripts/tests/` — the script previously had NONE. `conftest.py` stubs `uptime_kuma_api` in `sys.modules` because the real package is not installed locally and the module `sys.exit(1)`s on ImportError; production import path untouched. **NOT live-verified.** The acceptance criterion is inherently a live one and is operator-gated: two consecutive real `kuma-sync` runs where the SECOND reports `updated: 0` and every monitor `unchanged`. Anything less means the permanent diff is still there. The reviewer additionally recommends a wedged-Kuma run to confirm the budget aborts as fast as intended. Note the ordering value: this unblocks INF-3's remaining prune dispatch, and `app_filter=webapp-management` remains the narrow-blast-radius option for that. **Separate defect found while mapping, NOT fixed here (out of scope, needs its own WO + operator decision):** because `sync_monitors_multi` never resolves `notification_name`, monitors synced by the CI path appear to get NO relay notification assigned at all — a monitor that goes down would alert nobody. Potentially more serious than the defect CI-4 fixes. **Root cause PROVEN against the installed fork, not inferred** (2026-08-06): `uptime-kuma-api-v2` **1.0.3** introspected directly in a scratch venv. `UptimeKumaApi.get_monitors` ends with `_convert_monitor_return(monitor)` / `int_to_bool(r, ["active"])` / **`parse_monitor_type(r)`** / `parse_auth_method(r)`, so `type` IS converted to a `MonitorType` enum on every read. `MonitorType` is a `(str, Enum)`: `str(MonitorType.HTTP)` == `MonitorType.HTTP` while `.value` == `http`, so the old comparison evaluated False (= differs) for EVERY monitor and the new one evaluates True. The same read path confirms `_convert_monitor_return` normalises `notificationIDList` dict->list[int], independently confirming the Envelope's suspect was wrong. **Second-cause candidate ruled out:** `accepted_statuscodes` is declared only by hpc-bridge (4 monitors) and webapp-management (health monitors), always as `["200"]`, which round-trips unchanged (the library only substitutes a default when the value is falsy). So `type` is the single cause, and it hits 100% of monitors because every monitor has one. **The fix is LIVE IMMEDIATELY — no tag or pin bump needed:** `webapp-management/.github/workflows/kuma-sync.yml` checks out this repo at `ref: main` and runs `_workflow-templates/scripts/kuma_sync.py multi` directly (contrast INF-5, which was inert behind the collection SHA pin). **The acceptance test therefore self-serves:** kuma-sync is scheduled `0 */6 * * *`, so two consecutive scheduled runs within ~12 h satisfy the WO's criterion with no dispatch. Expected signature: the FIRST post-fix run should already report `updated: 0` with every monitor `unchanged`, unless something genuinely drifted — anything else means the permanent diff was not fully closed, and the new scope-0 `diff on '<key>'` log lines will name the remaining key.|
| TS-1 | Tailnet join resilience | staging-health raw join → tailnet-connect probe-guard; `use-cache` + re-probe-gated retry on cold joins; internal composite refs → v2.5.2 | 2026-07-16 | done | 67ba66f | Phase 1 on `main` (10 files), reviewed independently, no findings; tag `v2.5.2` = `2c59eab` (register commit on top of `67ba66f`, self-refs verified). Phase 2 = webapp-management `CI-1` (`a312525` + `0c581ef`, 8 pins); first live cold-join green with cache seeded (deploy-traefik run 29522808584: probe false → CDN download → "Cache saved … tailscale-1.98.4", no "Unexpected input" warning — subsequent runs restore from actions/cache). Guardrail: never `tailscale reset` / unconditional `up` on the shared daemon (regression `40c70c4`, known-good `e196e6c`). deploy-app deliberately NOT converted (pinned `@v2.4.4` by app repos; convert next time it is touched) |
| CI-1 | Unique per-run CI image tag | app-ci backend job: replace the fixed `ci-backend:test` with `ci-backend:${{ github.run_id }}-${{ github.run_attempt }}` in build-push tags AND the `docker run` pytest step, plus an `if: always()` `docker rmi` cleanup step — the fixed tag races between concurrent backend jobs on the shared daemon and executes the WRONG repo's tests | 2026-07-17 | done | 53e862c | Proven cross-repo execution: jg-ferien PR #71 (run 28984827793, 00:16:39Z) collected hram's `runs/tests/test_golden_regression.py`, hram CI started 34s later; hram develop CI red 07-15/16 was MIXED: the 08:40 run executed survey_app's `surveys/test_ws_inventory.py` (image race), while runs after hram's 07:13 engine-v1.12.0 bump (`4c0e6ea0`) also fail on hram's OWN `calculate/` tests — a real regression tracked separately in hram; post-fix hram runs stay red on OWN tests until that lands. app-ci is consumed `@main` → fix is fleet-live on push. Companion issue (app repos, separate WO): jg-ferien test_settings lacks CHANNEL_LAYERS/CACHES overrides (dcm 2.25 defaults → `redis://redis` unresolvable under `--network host`). Residual assumption (implementer+reviewer+orchestrator concur): tag uniqueness relies on NO matrix on the backend job — verified for all 13 current callers; a future matrix must add `strategy.job-index` to `CI_IMAGE`. Orchestrator post-landing review 2026-07-17: clean, no findings |
| CI-2 | Maintenance action: no false failure on reboot / apt sshd restart | The `maintenance` composite action (`.github/actions/maintenance/action.yml`) red-flags a run (exit 255, "connection closed by remote host") when a target box legitimately reboots or when `apt-get upgrade` restarts sshd mid-session — the bare `ssh` calls in the apt step (`if apply_updates`) and the decide step (trailing `ssh "uname -a; uptime"`) run under `set -euo pipefail` with no reconnect tolerance. Make the run finish GREEN when the box returns healthy with expected containers present; red only on genuine problems (box never returns within the wait timeout, expected container tokens missing, real apt error). | 2026-07-19 | done | 07897d5 | Root cause of the recurring webapp-management "production deploy failed" mail (cockpit mislabels any failed main workflow). Evidence: webapp-management run 29676082177 (2026-07-19 06:13 UTC), all 3 prod targets X on the maintenance step, `Permanently added to known hosts` → `Connection closed by remote host` → exit 255 (early ssh). Code fix landed on `main` (`07897d5`): apt upgrade decoupled from the SSH session (detached `setsid nohup` + status-dir marker file, polled by a new reconnect-tolerant "Wait for system updates to finish" step); decide-step's trailing informational `uname -a; uptime` hardened with `\|\| true`. Independent review: found + fixed a P1 (apt-run exit code was captured from the trailing group command, not the actual failing command — masked real apt failures as green; fixed via an inner `( set -e; … )` subshell with its own captured `$?`), re-reviewed and confirmed fixed. Tagged `v2.5.3` (= `286ac59`, dereferenced-verified on remote); webapp-management pin bumped `@v2.5.2` → `@v2.5.3` (`f9561ae`, one-line diff independently reviewed, no other stray pin found, input contract unchanged). Staging registry had no `maintenance` role (only `*-prod` boxes did) — temporarily added (`6ed4eb3`), live-dispatched (`gh workflow run maintenance.yml --field target=staging --field apply_updates=true --field force_reboot=false`, run `29682171283`), reverted after (`cb791e1`). Live run fully exercised the fix end-to-end in one go: staging genuinely had 25 upgradable packages incl. a kernel bump → apt ran backgrounded through the actual sshd restart (no false-red) → `reboot-required` detected → reboot triggered → host reconnect-waited successfully → post-check found all expected containers — run concluded green. `force_reboot=true` dry-run skipped as redundant (reboot/wait/post-check path was already proven live by the natural reboot). Prod runs next hit this fix on the regular Sunday 01:00 UTC cron (2026-07-26) or on-demand dispatch. |
| CI-3 | Shift-left secret-scan + dep-audit gate | New `security` job in app-ci.yml (reusable `workflow_call`, fleet-wide via `run-security` input, default true): blocking gitleaks secret scan + pip-audit/pnpm-audit report-only (continue-on-error). Companion pre-commit config (gitleaks + detect-secrets) shipped to webapp-template + piloted in survey_app (separate WOs there). | 2026-07-29 | done | 7fbbb42, 33ac3ce | reviewer + sec_reviewer: no open findings after fixes. Live `workflow_dispatch` validation on webapp-template caught 2 real defects missed by static review, both fixed + re-validated live before relying on this fleet-wide: (1) `gitleaks/gitleaks-action` now requires a paid `GITLEAKS_LICENSE` for GitHub orgs (breaking upstream change) — replaced with a direct, SHA256-verified download of the free OSS `gitleaks` CLI; (2) sec_reviewer S1: the replacement's first cut (`gitleaks dir .`, working-tree only) would miss a secret added then removed within the same PR (still lands permanently in merged history/blame) — empirically confirmed the gap with a synthetic add-then-remove-secret test, then fixed to `gitleaks git . --log-opts=<PR base..head, or HEAD~1..HEAD on non-PR triggers>`. Final live re-dispatch (run 30450585006): security/backend/frontend all green. |
| INF-5 | Stop the provisioning play from upgrading the transport it runs over | Collection half of webapp-management's INF-5 (same WO, two repos). The `base` role's "Apt update and upgrade" task upgraded the `tailscale` package; its postinst restarts `tailscaled`, which serves the Tailscale-SSH session Ansible connects through, so the play terminated its own connection and every first `ansible-provision` run against a host failed `UNREACHABLE`. New `base_apt_hold_packages` (default `[tailscale]`): dpkg-held across the apt upgrade, released in `always`, only for installed packages. Upgrade path unchanged — the weekly `maintenance` workflow and the runner's local os-maintenance timer both run apt detached and tolerate the session drop. Full order: [`webapp-management/work-orders/INF-5.md`](https://github.com/bigler-webapps/webapp-management/blob/main/work-orders/INF-5.md) | 2026-08-06 | done | 43b4f41 | Tier 2, prod-infra. Consumed by SHA pin (`webapp-management/ansible/requirements.yml`), so this change is inert until that pin is bumped. galaxy version 0.5.1 -> 0.5.2 (patch: behaviour fix to an existing role plus one knob, not a new capability area). Landed 2026-08-06 together with the consumer half (webapp-management d41023a, which bumps the pin f91f71a->43b4f41 — this change is inert until that bump). Codex was out of workspace credits on both chunks, so the Codex->Claude fallback applied and an independent `reviewer` was mandatory: no P1, one P2 (R1) FIXED — `always` cannot run if the Ansible controller dies (CI cancellation, runner crash), which would leave a real dpkg hold behind that BOTH designated upgrade paths respect silently; closed by arming a host-side `systemd-run --on-active=60min /usr/bin/apt-mark unhold <pkg>` before anything can fail, so the worst case is bounded at one hour instead of indefinite. Anonymous unit (no `--unit`) so back-to-back provisions cannot collide, never disarmed because a later firing is a no-op on an unheld package. 7 structural pytest cases in `ansible/roles/base/tests/` (hold precedes the upgrade, release sits under `always`, hold and release cover the same set, only installed packages are held, safety net armed before the upgrade). No live Ansible verification — no ansible/ansible-lint on the maintainer machine; the acceptance test is an operator-gated `ansible-provision` against a host with a PENDING tailscale upgrade. **Live smoke run on staging 2026-08-06** (webapp-management run 31094650853): green first attempt, `ok=109 changed=6 unreachable=0 failed=0`. All six new/changed tasks executed in order; `apt-mark showhold` empty afterwards; the anonymous safety-net timer armed with ExecStart `/usr/bin/apt-mark unhold tailscale`; `tailscaled` start time unchanged from the 2026-08-05 incident, i.e. the play no longer restarts its own transport. Runtime deps confirmed present on a real host (python3-apt importable, systemd-run, apt-mark). NOT the acceptance test — staging had no pending tailscale upgrade, so the hold was not load-bearing in this run.|
